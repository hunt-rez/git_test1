/*
// код выводит текст
var mom = {
    sex: prompt("Введите пол Мамы")
};

var dad = {
    sex: prompt("Введите пол Папы")
};

dad.sex === 'male' && mom.sex==='female'?
   alert('they can have a baby')
    :alert('they cant have a baby');
*/

/*
let b = Math.random() * 5 - 2
let c =(Math.random() * b).toFixed(3);

function count () {

    if  (c >= 0) {
        console.log('Первоначальное положительное число '+ c);
        c++}

    if (c < 0) {
            console.log ('Первоначальное отрицательное число '+c);
    c++;}
    console.log('Конечное число '+c.toFixed(3));
    }
count();

*/

